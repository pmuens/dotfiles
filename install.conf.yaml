- defaults:
    link:
      relink: true

- clean: ["~"]

- link:
    ~/.gitconfig:

- create:
    - ~/code/pmuens/pmuens

- shell:
    - [git submodule update --init --recursive, Installing submodules]
